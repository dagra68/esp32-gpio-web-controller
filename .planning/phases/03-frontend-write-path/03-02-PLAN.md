---
phase: 03-frontend-write-path
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - WRITE-01

must_haves:
  truths:
    - "Toggle button appears on exactly 9 OUTPUT cards (GPIO 0,1,3,4,5,6,7,10,11) and on zero INPUT cards (GPIO 20,21)"
    - "Clicking Toggle on any output card changes the physical pin state — confirmed by multimeter or LED"
    - "No toggle button is visible on GPIO 20 or GPIO 21 cards"
    - "With browser SSE open and HA native_api connected, device runs 10 minutes without watchdog reset"
    - "Failed POST (device unreachable) shows a visible error on the affected card"
  artifacts:
    - path: "D:/_vibecoding/_claude/esp32-gpio-controller/gpio-ui.js"
      provides: "Complete write path implementation from Plan 03-01"
      contains: "inFlight"
    - path: "D:/_vibecoding/_claude/esp32-gpio-controller/index.html"
      provides: "Toggle CSS from Plan 03-01"
      contains: ".toggle-btn"
  key_links:
    - from: "Browser http://10.1.1.162"
      to: "Toggle button on switch cards"
      via: "buildCard() conditional toggleBtn"
      pattern: "visual — 9 buttons, 0 on inputs"
    - from: "Toggle click"
      to: "POST /switch/GPIO%20N/toggle HTTP 200"
      via: "sendToggle() fetch"
      pattern: "DevTools Network tab"
---

<objective>
Verify the complete write path on live hardware and confirm concurrent load stability.

Purpose: This plan is the phase gate — no code is written here. All three Phase 3 success criteria are verified by the user on the live device. Concurrent load test (10 minutes) is the final confirmation before phase sign-off.
Output: Human-verified WRITE-01, signed-off phase summary.
</objective>

<execution_context>
@C:/Users/Daniel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/_vibecoding/_claude/.planning/ROADMAP.md
@D:/_vibecoding/_claude/.planning/phases/03-frontend-write-path/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify toggle buttons and write path on live device</name>
  <what-built>
    Plan 03-01 added toggle buttons to output pin cards in gpio-ui.js and toggle CSS to index.html. The button fires a POST to /switch/GPIO%20N/toggle via fetch(), guarded by an inFlight Set.
  </what-built>
  <how-to-verify>
    Device IP: 10.1.1.162. Perform all steps in order.

    Step 1 — Confirm buttons appear only on OUTPUT pins:
      Open http://10.1.1.162 in browser. Wait for SSE to connect (status banner disappears).
      Count cards with a "Toggle" button: expected exactly 9 (GPIO 0, 1, 3, 4, 5, 6, 7, 10, 11).
      Confirm GPIO 20 and GPIO 21 cards have NO button.

    Step 2 — Confirm toggle changes pin state:
      Click "Toggle" on GPIO 3 (or any output pin with a known state).
      Expected: the HIGH/LOW badge on that card flips within ~1 second (SSE state event received).
      If you have a multimeter or LED on that pin: confirm the physical voltage changes.

    Step 3 — Confirm in-flight guard (click rapidly):
      Click the same Toggle button 5 times quickly.
      Expected: button goes disabled after first click, subsequent clicks have no effect.
      Expected: only ONE POST appears in DevTools Network tab for that pin (not 5).
      To check: open DevTools (F12) -> Network tab -> filter by "toggle" -> click rapidly.

    Step 4 — Confirm error feedback (optional — requires unplugging device):
      If you can safely disconnect the device from power briefly, open the UI first, then unplug.
      Click any Toggle button while device is unreachable.
      Expected: "Unreachable" text appears on that card for ~3 seconds.
      Reconnect device — SSE reconnects, error clears, buttons work again.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass, or describe which step failed and what you observed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 10-minute concurrent load stability test</name>
  <what-built>
    The ESP32-C3 YAML has api: reboot_timeout: 0s and runs both the web_server (SSE) and native_api simultaneously. This test confirms the device stays stable under realistic concurrent load for the full v1 use case.
  </what-built>
  <how-to-verify>
    Prerequisites: Home Assistant must have the ESP32-C3 device configured and connected via native_api (port 6053). If HA is not available, skip to the pass/fail note at the bottom.

    Step 1 — Start ESPHome log monitoring:
      In a terminal, run:
        esphome logs D:/_vibecoding/_claude/esp32-gpio-controller/gpio-controller.yaml --device 10.1.1.162
      Keep this terminal visible throughout the test.

    Step 2 — Establish concurrent load:
      Browser tab: open http://10.1.1.162 — confirm SSE connected (no banner).
      Home Assistant: confirm device shows as available/connected in HA device list.

    Step 3 — Run for 10 minutes:
      Leave both connections open. Optionally click Toggle 2-3 times at the 2-minute and 5-minute marks to exercise the write path under load.
      Do not close the browser tab or HA connection during the test.

    Step 4 — Check log output:
      PASS indicators: no lines containing "Task watchdog got triggered", "Guru Meditation Error", or "ESP-IDF Version" (reboot line) during the 10-minute window.
      FAIL indicators: any of those strings appear in the log.

    If stability test fails:
      1. Edit gpio-controller.yaml: change `logger: level: DEBUG` to `logger: level: WARN`
      2. Run: esphome run gpio-controller.yaml --device 10.1.1.162
      3. Re-run this test. WARN level significantly reduces RAM usage.

    If HA is unavailable:
      Run the test with browser SSE only (no native_api). Note "HA not available" in your resume signal. Phase passes for WRITE-01 purposes — native_api stability is a best-effort check, not a blocker.
  </how-to-verify>
  <resume-signal>
    Type "stable" if 10 minutes passed with no watchdog resets, or "failed: [paste log lines]" if the device rebooted, or "no-ha: [browser-only result]" if HA was not available.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
- Task 1: Toggle buttons visible on 9 output cards, absent on 2 input cards, POST fires on click, SSE state update reflects new state
- Task 2: 10-minute concurrent load test passes (no watchdog resets in ESPHome logs)

ROADMAP.md Phase 3 success criteria mapped to verifications:
  1. "Clicking toggle sends REST POST and badge updates" -> Task 1 Step 2
  2. "Failed POST shows error indication" -> Task 1 Step 4
  3. "Device stable 10 minutes under concurrent load" -> Task 2
</verification>

<success_criteria>
All three Phase 3 ROADMAP success criteria confirmed on live hardware:
1. Toggle button clicks send REST POST, pin badge updates via SSE state event
2. Failed POST (device unreachable) shows inline error message — no silent failure
3. Device stable for 10 minutes with browser SSE + HA native_api concurrent connections
</success_criteria>

<output>
After completion, create `D:/_vibecoding/_claude/.planning/phases/03-frontend-write-path/03-02-SUMMARY.md` using the standard summary template.

Also update REQUIREMENTS.md: mark WRITE-01 as complete with date 2026-03-16.
Also update STATE.md: set status to phase_complete, Phase 3 complete.
</output>
